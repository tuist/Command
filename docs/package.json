{
  "name": "@tuist/command-docs",
  "private": true,
  "devDependencies": {
    "vitepress": "^1.1.0",
    "wrangler": "^4.0.0",
    "esbuild": "0.25.0",
    "cookie": ">=0.7.0"
  },
  "scripts": {
    "dev": "vitepress dev .",
    "build": "vitepress build .",
    "preview": "vitepress preview .",
    "deploy": "vitepress build && wrangler pages deploy .vitepress/dist --project-name tuist-command --branch main"
  }
}
